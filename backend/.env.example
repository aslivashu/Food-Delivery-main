MONGO_URL=your_mongodb_connection_string_here
JWT_SECRET=replace_with_secure_random_string
SALT=10
STRIPE_SECRET_KEY=sk_test_xxx
FRONTEND_URL=https://your-frontend-url
PORT=4000

# NOTE: Do NOT commit real secrets. Copy these into a local `.env` when running locally,
# and set them as environment variables in your cloud host (Render, Railway, etc.).
